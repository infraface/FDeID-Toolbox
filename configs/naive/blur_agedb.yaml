# Naive De-identification: Gaussian Blur on AgeDB
# Usage: python scripts/run_naive_deid.py --config configs/naive/blur_agedb.yaml

dataset: agedb
method: blur
kernel_size: 60

# Pretrained model paths
retinaface_model: ./weight/retinaface_pre_trained/Resnet50_Final.pth

save_dir: runs/inference/blur_agedb
device: cuda
