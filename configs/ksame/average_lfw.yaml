# K-Same De-identification: k-Same-Average on LFW
# Usage: python scripts/run_ksame_deid.py --config configs/ksame/average_lfw.yaml

dataset: lfw
method: average
k: 10

# Pretrained model paths
retinaface_model: ./weight/retinaface_pre_trained/Resnet50_Final.pth

save_dir: runs/inference/ksame_average_lfw
device: cuda
