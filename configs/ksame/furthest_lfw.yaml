# K-Same De-identification: k-Same-Furthest on LFW
# Usage: python scripts/run_ksame_deid.py --config configs/ksame/furthest_lfw.yaml

dataset: lfw
method: furthest
k: 10

# Pretrained model paths
retinaface_model: ./weight/retinaface_pre_trained/Resnet50_Final.pth

save_dir: runs/inference/ksame_furthest_lfw
device: cuda
